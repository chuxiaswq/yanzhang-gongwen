{
  "title": "砚章·AI文字工作台 MCP 连接",
  "title_en": "Yanzhang AI Writing Workbench MCP Connection",
  "description": "填写部署端生成的独立 MCP 访问令牌。凭据只由本机 WorkBuddy 连接配置保存。",
  "description_en": "Enter the dedicated MCP access token generated by your deployment. WorkBuddy stores it in the local connector configuration.",
  "fields": [
    {
      "key": "YANZHANG_MCP_ACCESS_TOKEN",
      "label": "MCP 访问令牌",
      "label_en": "MCP Access Token",
      "type": "password",
      "required": true,
      "placeholder": "从服务器的部署环境文件中获取",
      "description": "作为 Bearer 凭据连接砚章 `/mcp`；它与 Web Token、模型供应商密钥彼此独立。",
      "description_en": "Used as the Bearer credential for Yanzhang /mcp; it is separate from the Web token and model-provider keys."
    }
  ]
}
