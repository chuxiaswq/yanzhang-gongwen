# 砚章·AI文字工作台安全策略

## 支持范围

| 版本 | 状态 |
| --- | --- |
| `0.2.x` 预览版 | 接收安全修复 |
| `0.1.x` 预览版 | 提供升级兼容路径，建议迁移到最新预览版 |
| 更早的开发快照 | 建议先升级并在新版本复现 |

## 私下报告安全问题

请使用 GitHub 仓库 **Security → Report a vulnerability** 的私密入口，并提供：受影响版本、
入口类型（Web、API、MCP、连接器或文件解析）、最小复现、影响判断和建议修复方向。

提交前请完成脱敏：

- 使用占位域名、占位令牌、虚构项目和最小文稿；
- 删除真实姓名、单位、内部文件、文献全文、数据库、备份和日志；
- 删除 API 密钥、访问令牌、Cookie、认证请求头及服务器地址；
- 截图遮盖地址栏、账号、文稿内容和本机文件路径。

凭据一旦进入提交、构建产物、Issue 或日志，应立即在对应服务轮换，并清理已公开的历史对象；
只删除当前版本中的值不足以覆盖已经复制或缓存的历史内容。

## 部署基线

- 公网入口使用 HTTPS；Web 与 MCP 分别配置不同的高强度随机令牌。
- 任何环境只要应用监听非回环地址，启动前都校验 Web/MCP 边界；Web 公开访问需要显式开关，
  MCP 仍保留独立且至少 32 字节的令牌。命令行 `--host` 覆盖同样执行该校验。
- 首选 `YANZHANG_ACCESS_TOKEN` 与 `YANZHANG_MCP_ACCESS_TOKEN`；升级期的 `GONGWEN_*`
  同名变量仍可读取，同时存在时采用 `YANZHANG_*`。
- `.env` 权限保持为 `0600`；模型密钥放在环境、容器 Secret 或密钥管理工具中，不进入镜像、
  数据库、模型画像、连接器包或客户端配置仓库。
- `YANZHANG_ALLOWED_HOSTS` 只列实际域名；CORS 只列确需跨站调用的完整 HTTPS Origin；可信
  代理列表只包含真实反向代理网络。
- SQLite 部署保持单应用工作进程。使用独立持久卷，定期创建加密异机备份并演练恢复。
- 页面自定义模型地址只允许已审核的精确 HTTPS 地址；生产服务端 Provider 同样默认要求
  HTTPS，仅显式开启后接受回环地址上的本地 HTTP 模型。
- 访问日志保持默认关闭；开启时使用省略查询参数与客户端信息的应用日志，并采用访问控制和
  短期保留。
- 人民网自动检索涉及 HTTP 上游，维持默认关闭；手工 HTTPS 导入和其他 HTTPS 来源照常使用。

## API 与 MCP

- `/api/v2/*` 与兼容 `/api/*` 共享请求体上限、Host 校验、速率限制、安全响应头和 Web Bearer
  鉴权；`/mcp` 使用独立 MCP Bearer 鉴权。
- 所有含正文的 POST 接口只接收 `application/json`；浏览器简单表单或 `text/plain` 请求在解析
  前返回 `415`，避免跨站页面绕过预检触发写入或模型请求。
- 反向代理保留 MCP 所需的 `Authorization`、`Accept`、`Content-Type`、
  `Mcp-Session-Id`、`MCP-Protocol-Version` 与 `Last-Event-ID` 请求头。
- 远程 MCP 使用无状态 Streamable HTTP；stdio 适合本机受信任客户端。工具参数只包含任务
  数据和资源标识，不包含模型密钥。
- 删除、发布、覆盖版本和外部查询属于有副作用操作，客户端应在调用前展示目标与范围。
- 项目、知识资料、文字资产和证据标识在服务端重新校验，不把客户端显示文本当作权限边界。

## 文件、来源与插件

- BibTeX、RIS、CSL-JSON、PDF、DOCX、文章页面和插件输出都按外部输入处理，并受类型、大小、
  数量和解析深度限制。
- 公共解析入口只接收调用方提供的字节，不读取调用方路径、不自行联网，也不跟随 HTML、DOCX
  或 PDF 外链。默认限制为 12 MiB 输入、200 万字符、单块 20 万字符、10000 个块和 500 页 PDF。
- DOCX 最多 512 个 ZIP 成员、单成员 8 MiB、解压合计 32 MiB、成员名 512 字符，只处理 stored
  或 deflate；路径穿越、重复名、加密、链接/特殊成员、宏、ActiveX、嵌入对象、外部/活动关系、
  DTD/ENTITY 和活动字段均会被拦截。
- HTML 拦截脚本、iframe、object、embed、form、style、base、事件属性、外部 URL/srcset、
  meta refresh、srcdoc 和危险行内样式。
- PDF 解析器按需加载 `pypdf`，拦截加密、自动动作、JavaScript、嵌入文件、XFA/活动表单、附件、
  活动媒体以及 URI/Launch 等动作。
- 来源连接器限制协议、主机、重定向和响应体大小；自定义模型 URL 必须命中部署端白名单。
- 第三方扩展通过明确的 entry point 类别注册。部署前应审查依赖、网络访问、数据范围和发布目标，
  并在隔离环境执行模拟数据测试。
- 通用导出器在内存中生成结果，默认最多 200 万字符/20 MiB，配置硬上限 100 MiB。HTML 转义
  内容并附 CSP/no-referrer；LaTeX/Markdown 转义控制字符；CSV 使用 UTF-8 BOM 并中和公式
  注入；PDF 按需加载 `reportlab` 和 STSong-Light CID 字体。
- 导出文件名、资源 URI 和对象标识不直接映射调用方提供的文件路径；持久化、保留、删除与发布
  权限由入口层和运维策略控制，下载后仍按组织文档策略检查。

## 学术完整性

来源哈希、页码/段落/字符定位和引用覆盖率用于发现错配、缺引与内容变化，不替代作者、导师、
审稿人或期刊的专业判断。公开元数据连接器返回记录才可标记 `metadata_verified`；DOI 规范化、
格式化参考文献和模型语义评分都不单独构成真实性证明。方法、统计结果、直接引语和核心结论在
发布前逐项回到原文复核。

安全相关变更记录在 [CHANGELOG.md](CHANGELOG.md)，部署操作见
[docs/operations.md](docs/operations.md)。
