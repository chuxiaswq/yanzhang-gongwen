# 安全策略

## 支持范围

| 版本 | 状态 |
| --- | --- |
| `0.1.x` 预览版 | 接收安全修复 |
| 更早的开发快照 | 请先升级到最新预览版 |

## 私下报告安全问题

请使用 GitHub 仓库 **Security → Report a vulnerability** 的私密报告入口。报告中请包含受影响版本、复现步骤、影响判断和建议修复方向。

提交材料前请完成脱敏：

- 使用占位域名、占位令牌和最小复现文稿；
- 删除真实姓名、单位、内部文件、文章库、数据库和日志；
- 删除 API 密钥、访问令牌、Cookie、请求头及服务器地址；
- 截图需要遮盖浏览器地址栏、账号和文件路径。

若凭据曾进入提交、构建产物、Issue 或日志，应立即在对应服务轮换，并清理已公开的历史对象。仅从最新提交中删除不足以处理已进入历史记录的内容。

## 部署基线

- 公网环境使用 HTTPS，并为 Web 与 MCP 配置不同的高强度随机令牌。
- 将 `.env` 权限保持为 `0600`，不把模型密钥写入镜像或客户端配置仓库。
- 仅将必要域名加入 `GONGWEN_ALLOWED_HOSTS` 与 CORS 白名单。
- 保持单应用工作进程和独立持久卷，定期创建加密异机备份并演练恢复。
- 页面自定义模型地址仅允许已审核的 HTTPS 精确地址。
- 人民网自动检索保持默认关闭；评估网络路径后再显式启用。
- 应用访问日志保持默认关闭；仅在已配置访问控制、脱敏和短期保留时临时开启。

安全修复会记录在 [CHANGELOG.md](CHANGELOG.md)，但在修复发布前不会公开可利用细节。
