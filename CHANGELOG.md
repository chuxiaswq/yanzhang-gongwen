# 更新记录

本项目采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的组织方式，版本号遵循语义化版本与 PEP 440 预发布规则。

## [0.2.0b1] - 2026-09-04

### 新增

- 产品升级为“砚章·AI文字工作台”，新增公文与综合材料、职场沟通、内容传播、学术与研究写作四个场景包，共提供 17 个透明可检查的写作配方。
- 建立项目—任务简报—知识资料—母稿—渠道变体—证据链—版本快照的通用写作对象模型；文字资产使用有序内容块，便于局部改写、锁定和追溯。
- 新增 `/api/v2/*` 项目、资料、术语、写作工作流、文字资产与审校接口，并提供 45 个独立的 `yanzhang_*` MCP 工具，其中 21 个覆盖学术研究流程。
- 新增 BibTeX、RIS、CSL-JSON 双向处理，Crossref、OpenAlex、arXiv 元数据连接器，PDF/DOCX 文本提取，文献矩阵、论断—证据核验、参考文献格式化、研究提纲、摘要与审稿回复流程。
- 新增 Provider 中立扩展点：来源连接器、解析器、工作流步骤、模板包、审校器、导出器和发布目标。
- 通用成稿与工作流持久化 `Evidence → Claim → Citation` 证据链；六维审校可计算证据覆盖率，
  引用矩阵 CSV 包含资料 ID、定位与来源哈希。
- 新增 `yanzhang-web`、`yanzhang-mcp` 命令及 `YANZHANG_*` 首选配置名。

### 变更

- Web 导航改为任务入口、项目工作台、知识资料、写作配方、审校中心、交付中心、学术研究和设置中心。
- 审校输出统一围绕事实、结构、语言、引用、格式与交付完整性六个维度，并保留可核查主张、证据摘录、来源定位与版本关系。
- WorkBuddy Connector 更新至 `0.2.0-preview.1`，默认引导使用新的项目化与多场景工具，同时保留原公文快捷流程。
- 文档、部署样例和运行配置以 `YANZHANG_*` 为主名称。
- 组合根会发现并编目全部七类通用扩展，自动注入并可实际执行 `workflow_step`；其余六类
  作为可发现 SDK 工厂供显式集成使用。
- 新部署入口统一为 `deploy/gongwen/scripts/{start,health,backup,restore,upgrade}.sh`；原脚本入口与
  `gongwen-web-data` 命名卷继续沿用，支持就地备份升级。

### 兼容

- 原 `/api/*` 端点继续提供既有公文写作行为；新能力位于 `/api/v2/*`。
- 既有 26 个 `gongwen_*` MCP 工具、`gongwen://` Resources、Prompts 与命令入口继续有效。
- 同名后缀的 `GONGWEN_*` 环境变量继续读取；`YANZHANG_*` 与其同时存在时采用前者。
- 原 SQLite 数据目录和备份流程保持可用，升级前仍建议创建一致性备份。

### 安全与隐私

- Web 与 MCP 继续使用独立访问令牌；模型画像不保存供应商密钥。
- 非回环监听在所有运行环境中执行凭据校验，命令行 `--host` 覆盖也会在启动前复核；显式公开
  Web 时，远程 MCP 仍保留独立强令牌。
- 生产服务端模型端点默认要求 HTTPS；回环地址上的本地 HTTP 模型需通过独立开关显式启用，
  带用户信息、查询参数或片段的基础地址会在启动前被拒绝。
- 兼容 `/api/*` 的正文 POST 接口只解析 `application/json`，阻断浏览器简单表单跨站触发写入或
  模型请求。
- 可选访问日志改为应用内脱敏记录，只保留方法、路径、状态码和耗时，省略查询条件、客户端
  地址、请求头与正文。
- 学术证据记录增加来源哈希和页码、段落或字符定位；未知文献、来源错配、低支撑度和直接引语缺页码会进入人工复核队列。
- DOI 处理仅做格式规范化；公开连接器返回的记录才标记 `metadata_verified`，生成结果不把模型判断表示为来源事实。
- 文件解析、远程来源、模型端点、请求体、导出产物与插件入口继续受大小、协议、主机和发布白名单约束。

## [0.1.0b1] - 2026-09-04

### 新增

- 个人公文写作 Web 页面与 SQLite 持久化。
- 标题公式、方法论成稿、改写、审校和高级事实核对。
- 文章来源导入、范围采集、检索和风格引用。
- DOCX、MERGEFIELD 与批量 ZIP 导出。
- 26 个 MCP 工具、资源和 Prompt，支持 stdio 与 Streamable HTTP。
- WorkBuddy Connector 及 Trae、扣子等客户端接入示例。
- Docker/Caddy 部署、健康检查、备份和恢复工具。

### 安全

- Web 与 MCP 使用独立 Bearer Token，生产模式校验长度、占位值与重复使用。
- 构建采用公开发行白名单，加入敏感信息扫描、逐成员审计和 SHA-256 校验和。
- 默认数据目录迁移到操作系统个人应用数据目录，避免从当前目录误读数据。
- 人民网明文 HTTP 自动检索默认关闭，需显式配置后启用。
- 部署启动和健康检查避免在日志及进程参数中展示访问令牌。
