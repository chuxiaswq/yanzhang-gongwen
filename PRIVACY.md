# 隐私说明

## 数据保存在何处

砚章以本地优先方式运行。文稿、版本、文章来源、模型用量和导出索引保存在 SQLite 与本地导出目录中。默认位置为操作系统的个人应用数据目录：

- Linux：`${XDG_DATA_HOME:-~/.local/share}/yanzhang/gongwen`
- macOS：`~/Library/Application Support/Yanzhang/Gongwen`
- Windows：`%LOCALAPPDATA%\Yanzhang\Gongwen`

设置 `GONGWEN_DATA_DIR` 可将全部应用数据放到指定的私有目录。Docker 部署使用名为 `gongwen-web-data` 的持久卷。

浏览器会把当前草稿、事实材料、表单偏好、最多 12 份版本快照，以及已选文章来源的标题、
URL、摘要和写法特征持续保存在当前站点的 `localStorage`。这些内容在关闭标签页或浏览器后
仍会保留；文章来源全文保存在服务端 SQLite，不进入该浏览器缓存。

## 会离开设备的数据

只有启用相应功能时才发生外部请求：

1. **真实模型调用**：写作指令、所选材料、引用摘要或待审校正文会发往配置的模型 API。供应商如何保存这些请求由其服务条款和账户设置决定。
2. **文章来源采集**：服务会请求用户指定的文章 URL，或在所选来源与范围内执行检索。来源服务器会看到部署机器的网络地址和常规 HTTP 请求信息。
3. **远程 MCP**：MCP 客户端通过部署域名传入工具参数；HTTPS 终止层和服务器会处理这些内容。

项目不包含遥测、广告追踪或自动错误上报组件。

应用访问日志默认关闭，因为标准 HTTP 请求行可能包含文稿搜索词或文章来源查询条件。若运维
人员显式设置 `GONGWEN_ACCESS_LOG=true`，应限制日志访问、缩短保留时间，并在排障结束后
恢复默认值和清理相关日志。

## 凭据处理

- 服务器模型密钥通过环境变量注入，不写入数据库。
- 页面临时模型密钥只存在当前页面内存，刷新或关闭页面后清除。
- 网页访问令牌保存在浏览器 `sessionStorage`，关闭标签页会话后清除。
- MCP 与 Web 使用两枚不同令牌；部署日志不输出完整值。
- `show-token.sh` 仅在可信交互式终端工作，重定向或管道调用会终止。

## 保留与删除

文稿或文章可在界面/MCP 中单独删除。删除应用数据目录或 Docker 数据卷会移除本地运行数据；执行前应确认是否需要备份。备份、浏览器缓存和模型供应商侧记录具有各自的生命周期，需要分别管理。

清理浏览器副本时，在浏览器的“隐私与安全 / 网站数据”中找到当前访问地址（例如
`127.0.0.1` 或部署域名）并删除其站点数据；这会同时清除本地草稿、材料、快照、所选文章来源
缓存、页面偏好和当前会话令牌。共享设备在结束使用后应执行此操作。服务端删除不会联动清理
浏览器副本，清理浏览器也不会删除 SQLite、备份或模型供应商侧记录。

SQLite 行删除或普通文件删除不等同于介质级安全擦除。对高敏感内容，应分别处理数据库、
WAL/SHM 边车文件、导出文件和全部备份，并按所用设备与存储服务的安全擦除流程操作。

## 公开发行包

官方构建从固定白名单生成。以下内容被源代码、Docker 上下文和发布审计共同排除：

- `.env`、密钥文件和凭据配置；
- SQLite、文稿、文章、导出、备份与日志；
- 本机用户路径、虚拟环境、缓存和编辑器状态；
- 与砚章无关的源码、媒体和内部文档。

每次发布生成 `SHA256SUMS`，便于下载后核对完整性。
