# 砚章·AI文字工作台隐私说明

本说明适用于 `v0.2.0-preview.1` 的 Web、HTTP API、stdio MCP、Streamable HTTP MCP、
文章来源和学术资料处理功能。砚章以本地优先为默认原则；外部传输只发生在用户主动调用已配置
的模型或来源连接器时。

## 1. 本地保存的数据

### 服务端数据目录

项目、任务简报、知识资料、文字资产、内容块、版本快照、事实证据、文章来源、文献记录、
证据摘录、引用关系、审校结果、模型用量元数据与导出索引保存在 SQLite 和本地导出目录。
默认位置为：

- Linux：`${XDG_DATA_HOME:-~/.local/share}/yanzhang/gongwen`
- macOS：`~/Library/Application Support/Yanzhang/Gongwen`
- Windows：`%LOCALAPPDATA%\Yanzhang\Gongwen`

`YANZHANG_DATA_DIR` 可指定独立私有目录；兼容变量是 `GONGWEN_DATA_DIR`，两者同时设置时
`YANZHANG_DATA_DIR` 优先。Docker 默认使用 `gongwen-web-data` 持久卷，卷名沿用旧名称以便
原部署原地升级。

### 浏览器数据

浏览器 `localStorage` 会保存当前任务简报、母稿和渠道变体草稿、事实材料、界面偏好、快速
快照，以及已选资料的必要摘要。此类内容在关闭浏览器后仍会保留。网页登录令牌保存在
`sessionStorage`，关闭当前标签页会话后清除；页面临时填写的模型密钥只停留在当前页面内存。

浏览器副本与服务端 SQLite 是两个独立存储层。删除其中一处不会联动清理另一处。

### 导出与备份

DOCX、ZIP、参考文献和其他交付文件是独立文件。MCP 临时导出默认具有有效期，服务端备份则按
运维人员的保留策略保存。删除正文记录不会自动删除已经下载的文件、备份或第三方服务记录。

v0.2 导出索引的 sidecar 会保存 `project_id`、`asset_id`、`revision_id`、创建操作、文件名、
媒体类型、大小、哈希与有效期，用于按项目校验下载；其中不保存模型密钥或正文副本。旧 v0.1
未分项目工件仍使用兼容读取入口，两类工件不会通过同一个公开 URI 混用。

## 2. 可能离开设备的数据

只有启用相应操作时才发生外部请求：

1. **真实模型调用**：当前任务指令、所选资料、待改写内容、证据摘要或待审文本会发送到所选
   模型 API。供应商的保存和训练规则取决于相应账户设置与服务条款。
2. **文章来源连接器**：服务会访问用户指定的 URL，或按关键词、来源、日期和数量范围检索。
   来源服务器会看到部署机器的网络地址和标准请求信息。
3. **学术元数据连接器**：Crossref、OpenAlex、arXiv 查询会发送题名、作者、DOI、检索词或
   其他由用户提交的查询条件。导入本地 BibTeX、RIS、CSL-JSON、PDF 或 DOCX 不自动触发这些
   查询；只有用户启动查找或补全时才访问对应服务。
4. **远程 MCP**：客户端通过部署域名提交工具参数；客户端、HTTPS 终止层和砚章服务器会处理
   这些内容。MCP 参数中不应放模型密钥。
5. **自定义插件**：安装的来源连接器、解析器、工作流步骤、审校器、导出器或发布目标可能
   处理任务数据；其数据范围和去向由插件实现及部署者配置决定。

通用 TXT、Markdown、HTML、DOCX 和 PDF 解析入口只处理调用方提交的字节，不读取调用方提供的
本机路径、不自行联网，也不跟随文档外链。通用导出器先在内存生成字节，随后是否保存、下载或
发布由 Web/MCP 入口和用户操作决定。默认解析上限为 12 MiB 输入、200 万字符和 500 页 PDF；
默认导出上限为 200 万字符或 20 MiB。

项目本身没有遥测、广告追踪或自动错误上报组件。访问日志默认关闭；启用
`YANZHANG_ACCESS_LOG=true` 时，应用只记录请求方法、路径、状态码和耗时，不记录查询条件、
客户端地址、请求头或正文；仍应配置访问控制和短期保留，并在排障后清理。

## 3. 学术资料与引用真实性

砚章把文献元数据、原文证据和正文论断分层保存：

- `BibliographicRecord` 保存导入或公开连接器返回的著录信息；
- `EvidenceSnippet` 保存 `record_id`、来源哈希及页码、段落或字符位置；
- `ClaimCitationLink` 只在已传入的文献记录和来源哈希匹配的证据摘录之间建立核验关系。

这些机制提供来源链路，不代表对论文质量、原文含义或研究结论作出担保：

- DOI 只做大小写、URL 前缀和尾部标点规范化，不据此认定记录真实；
- 只有 Crossref、OpenAlex、arXiv 等公开连接器返回的元数据可设置
  `metadata_verified=true`；手工或文件导入记录仍需核对；
- 哈希能够发现证据来源错配或内容变化，不评价证据是否被正确解释；
- GB/T 7714、APA、MLA、Chicago 输出是基础著录格式，目标期刊的版本、标点和特殊规则应再
  核对；
- 研究方法、统计分析、直接引语、页码、作者、年份、卷期和最终结论保留人工复核节点。

## 4. 凭据处理

- 优先使用 `YANZHANG_LLM_API_KEY`、`YANZHANG_ACCESS_TOKEN` 和
  `YANZHANG_MCP_ACCESS_TOKEN`；同名 `GONGWEN_*` 变量作为升级兼容项。
- 服务器模型密钥由进程环境、容器 Secret 或密钥管理工具注入，不存入数据库、模型画像或
  文字资产。
- Web 与远程 MCP 使用两枚不同的随机令牌；生产值均至少 32 字节。
- 部署日志不输出完整令牌；`show-token.sh` 只面向可信交互式终端。
- WorkBuddy 等客户端只保存 MCP 访问令牌，不保存砚章服务器的模型供应商密钥。

## 5. 保留、导出与删除

- 项目、资料、文稿和文献可通过相应 Web/API/MCP 操作逐项管理；版本、证据链和导出文件按
  实际关系单独处理。
- 清理浏览器副本时，在浏览器“隐私与安全 / 网站数据”中删除当前站点数据；共享设备使用后
  建议执行此操作。
- 清理服务端时，同时检查 SQLite、`-wal`/`-shm` 边车文件、`exports/`、部署备份和下载目录。
- SQLite 行删除与普通文件删除不等同于介质级安全擦除。高敏感材料应依据设备、云盘和备份
  介质的擦除流程分别处理。
- 模型与学术来源供应商侧记录遵循各自的账户设置和保留规则，应在相应服务内单独管理。

## 6. 公开发行包

发行构建使用固定白名单，并审查 wheel、sdist 和 Connector 压缩包的每个成员。以下内容被源码
规则、Docker 上下文和发布审计排除：

- `.env`、访问令牌、API 密钥及个人凭据文件；
- SQLite、项目资料、文稿、文献全文、导出、备份与日志；
- 本机绝对路径、虚拟环境、缓存和编辑器状态；
- PDF、DOCX、图片、音视频及其他用户生成文件。

每次发行生成 `SHA256SUMS` 供下载后校验。部署和清理步骤见
[docs/operations.md](docs/operations.md)，安全报告渠道见 [SECURITY.md](SECURITY.md)。
